// two params position number and roll number of char between 1-6
// return the position times the roll * 2

function move(position, roll) {
	return position + roll * 2;
}

console.log(move(0, 5), 10);
